<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".complaint_management"
    android:orientation="vertical">

    <Button
        android:layout_width="match_parent"
        android:layout_height="120dp"
        android:id="@+id/register_new_complaint"
        style="@style/buttonDesign"
        android:text="Register New Complaint"
        android:background="@android:color/holo_orange_dark"
        android:textColor="@android:color/white">
    </Button>

    <Button
        android:layout_width="match_parent"
        android:layout_height="120dp"
        android:id="@+id/All_complaint"
        style="@style/buttonDesign"
        android:text="All Complaint"
        android:background="@android:color/holo_red_light"
        android:textColor="@android:color/white">
    </Button>

    <Button
        android:layout_width="match_parent"
        android:layout_height="120dp"
        android:id="@+id/Pending_complaint"
        style="@style/buttonDesign"
        android:text="Pending Complaint"
        android:background="@android:color/holo_blue_bright"
        android:textColor="@android:color/white">
    </Button>

</LinearLayout>